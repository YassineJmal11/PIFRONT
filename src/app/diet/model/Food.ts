export class Food {
    foodId!: number;
    foodName!: string;
    Cals!: number; // Change to match API response casing
    Carbs!: number; // Change to match API response casing
    prots!: number;
  }
  