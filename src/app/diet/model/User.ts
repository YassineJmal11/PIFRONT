export class User {
    userId!: number;
    username!: string;
    firstName!: string;
    lastName!: string;
    email!: string;
    phoneNumber!: number ; // Adjust type if necessary
    dateOfBirth!: string; // You might want to handle date conversion if needed
    gender!: string;
    weight!: number ; // Adjust type if necessary
    height!: number; // Adjust type if necessary
    profileImageUrl!: string ; // Adjust type if necessary
  }